get:
  summary: Retrieves a list of session ids.
  produces:
    - application/json
  parameters:
    $ref: './include2.yaml'
  responses:
    '200':
      description: OK
    '400':
      description: If end is less than start or start is less than 0.