## API Review File for "@codification/cutwater-core"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { Readable } from 'stream';

// @beta
export class Config {
    static get(key: string, defaultValue?: string): string;
    static getRequired(key: string, errorMsg?: string): string;
    static put(key: string, value: string): string;
}

// @beta
export class Env {
    static readonly DEFAULT_PROD_STAGE: string;
    static readonly ENV_PROD_STAGE: string;
    static readonly ENV_STAGE: string;
    static isDev(): boolean;
    static isProd(): boolean;
}

// @beta
export class IOUtils {
    static bufferToReadable(buffer: Buffer): Readable;
    static readableToBuffer(stream: Readable): Promise<Buffer>;
}

// @beta
export class StringUtils {
    static contains(value: string, searchTerm: string, caseInsensitive?: boolean): boolean;
    static endsWith(value: string, searchTerm: string, caseInsensitive?: boolean): boolean;
    static isBlank(value?: string): boolean;
    static isEmpty(value?: string): boolean;
    static startsWith(value: string, searchTerm: string, caseInsensitive?: boolean): boolean;
}

// @beta
export class TimeUnit {
    static days(count: number): TimeUnit;
    static hours(count: number): TimeUnit;
    static millis(count: number): TimeUnit;
    static minutes(count: number): TimeUnit;
    static seconds(count: number): TimeUnit;
    toDays(): number;
    toHours(): number;
    toMillis(): number;
    toMinutes(): number;
    toSeconds(): number;
}

// @beta
export class TZUtils {
    static readonly DEFAULT_OFFSET: number;
    static readonly ENV_OFFSET: string;
    static readonly FORMAT_TIMESTAMP: string;
    static readonly now: Date;
    static resetTimezoneOffset(): void;
    static timestamp(format?: string): string;
    static timezoneOffset: number;
}


// (No @packageDocumentation comment for this package)

```
